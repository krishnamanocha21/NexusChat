// import React from 'react'

// //in js s recieving component has no idea whaat is inside unit they open it but in ts interface acts like a transparent, strict label on that box
// //It tells the computer exactly what "ingredients" (props) this component needs to function

// interface SocicalButtonProps {
//     //the icon that will be displayed on the button
//   icon: React.ReactNode; //"I don't care exactly what this is, as long as React knows how to render it
//   onClick: () => void; //the function that will be called when the button is clicked
// }

// const 

// export default SocialButton;